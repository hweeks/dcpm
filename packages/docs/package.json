{
  "name": "@dcpm/docs",
  "version": "0.0.0",
  "description": "The docs site for dcpm",
  "author": "Harris Weeks <root@hweeks.com>",
  "homepage": "",
  "license": "ISC",
  "main": "lib/server.js",
  "publishConfig": {
    "access": "private"
  },
  "scripts": {
    "dev": "docker run --rm -it -p 8000:8000 -v ${PWD}:/docs squidfunk/mkdocs-material",
    "build": "docker run --rm --user \"$(id -u):$(id -g)\" -v ${PWD}:/docs squidfunk/mkdocs-material build && ./qd.sh"
  }
}
