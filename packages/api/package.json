{
  "name": "@dcpm/api",
  "version": "1.0.1",
  "description": "The api for dcpm",
  "author": "Harris Weeks <root@hweeks.com>",
  "homepage": "",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "prepublishOnly": "yarn build",
    "build": "tsc",
    "watch": "tsc -w",
    "watch:brk": "TS_NODE_FILES=true node --inspect-brk -r ts-node/register",
    "dev:start": "nodemon",
    "lint": "eslint lib/**/*.ts"
  },
  "dependencies": {
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.5.3",
    "nodemon": "^2.0.1",
    "ts-node": "^8.5.2",
    "typescript": "^3.7.2"
  }
}
